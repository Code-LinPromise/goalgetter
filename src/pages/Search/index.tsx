import { defineComponent} from "vue";
import s from "./style.module.scss";

export const Search= defineComponent({
   setup() {
     return () => {
       return (
         <div>
            hi search
         </div>
       );
     };
   },
});