import { defineComponent} from "vue";
import s from "./style.module.scss";

export const Add= defineComponent({
   setup() {
     return () => {
       return (
         <div>
            
         </div>
       );
     };
   },
});